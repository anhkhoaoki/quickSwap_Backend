
spring.application.name=quickswap-backend

# Use Render's $PORT (fallback to 8080 locally)
# server.port=${PORT:8080}

# Datasource (move secrets to environment variables on Render)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-wispy-meadow-a1tjlqaj-pooler.ap-southeast-1.aws.neon.tech/quizkSwap?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:neondb_owner}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:npg_xBNMcY92Xjgu}

spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration (set a strong secret in Render env)
jwt.secret=${JWT_SECRET:bagasdjhkthakhasdkkhkakhasdkkasdkhaskdhasdkyweirngasndkjashdajksdhaksjdhaksjdh}
jwt.expiration=86400000


# Cloudinary Config (provide real values as env vars in Render)
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:du48kdzdh}
cloudinary.api-key=${CLOUDINARY_API_KEY:726377896583184}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:gdBbbyPSNwrYKiVoHn8enr02Nso}

# Tăng giới hạn file upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB


# Mail (set SMTP creds via env vars on Render)
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:anhkhoaoki@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:vggawudwdjmcyrzv}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Firebase service account: store JSON in an env var `FIREBASE_SERVICE_ACCOUNT` and
# write it to a file at runtime or set `GOOGLE_APPLICATION_CREDENTIALS` to point to it.



